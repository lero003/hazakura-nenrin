# Example Nenrin Ledger

This sample shows the intended Markdown shape for a small agent-environment improvement.

For deciding whether a real repository needs a ledger at all, see
`../../docs/adoption_guide.md`.

The sample change is intentionally old enough to become review-due in real
dates. Treat that as example data, not as neglected project debt.
